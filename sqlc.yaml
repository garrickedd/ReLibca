version: "2"
sql:
-   schema: "./db/migration"
    queries: "./db/query/"
    engine: "postgresql"
    gen:
        go:
            package: "models"
            out: "./src/models/"
            emit_json_tags: true
            emit_pointers_for_null_types: true
            query_parameter_limit: 5


